{
  "name": "final-hackloop",
  "version": "1.0.0",
  "description": "Root package for Final Hackloop",
  "scripts": {
    "postinstall": "npm install --prefix backend",
    "start": "npm start --prefix backend"
  },
  "dependencies": {
    "htmx.org": "^2.0.8"
  }
}
